import { config as c } from './config';

const flex = {
  flex: c.toBase('flex'),
  flexBasis: c.toBase('flexBasis'),
  flexDirection: c.toBase('flexDirection'),
  flexFlow: c.toBase('flexFlow'),
  flexGrow: c.toBase('flexGrow'),
  flexShrink: c.toBase('flexShrink'),
  flexWrap: c.toBase('flexWrap'),
  alignItems: c.toBase('alignItems'),
  alignContent: c.toBase('alignContent'),
  alignSelf: c.toBase('alignSelf'),
  justifyItems: c.toBase('justifyItems'),
  justifyContent: c.toBase('justifyContent'),
  justifySelf: c.toBase('justifySelf'),
};

export default flex;
