export const size = {
  width: 'sizes',
  height: 'sizes',
  minWidth: 'sizes',
  minHeight: 'sizes',
  maxWidth: 'sizes',
  maxHeight: 'sizes',
};

Object.assign(size, {
  w: size.width,
  h: size.height,
  minW: size.minWidth,
  maxW: size.maxWidth,
  minH: size.minHeight,
  maxH: size.maxHeight,
});
