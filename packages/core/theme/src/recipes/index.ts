import { buttonRecipe } from './button';
import { iconButtonRecipe } from './iconButton';
import { SpinnerRecipe } from './spinner';

export const recipes = {
  button: buttonRecipe,
  iconButton: iconButtonRecipe,
  spinner: SpinnerRecipe,
};

export const slotRecipes = {};
