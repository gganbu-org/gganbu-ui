import { config as c } from './config';

const typography = {
  fontSize: c.fontSizes('fontSize'),
  lineHeight: c.lineHeights('lineHeight'),
  fontWeight: c.fontWeights('fontWeight'),
};

export default typography;
